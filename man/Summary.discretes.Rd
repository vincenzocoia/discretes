% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Summary.discretes.R
\name{Summary.discretes}
\alias{Summary.discretes}
\title{Summary group generic for numeric series}
\usage{
\method{Summary}{discretes}(x, ..., na.rm = FALSE)
}
\arguments{
\item{x, ...}{Numeric series and/or numeric vectors to summarise.}

\item{na.rm}{Single logical; passed to the underlying summary function when
combining values.}
}
\value{
A numeric vector: length 2 for \code{range()}, length 1 otherwise.
}
\description{
Support for \code{min()}, \code{max()}, \code{range()}, \code{sum()}, and \code{prod()} on numeric
series. \code{min()}, \code{max()}, and \code{range()} report the smallest and largest
values that bound the series: the extreme discrete values, together with any
\code{\link[=sinks]{sinks()}} (limit points) the discrete values approach. \code{sum()} and \code{prod()}
are only defined for series with finitely many discrete values.
}
\details{
\code{min()} and \code{max()} return the infimum and supremum of the discrete values.
For an unbounded series these are \code{-Inf} and \code{Inf}; for a series whose values
pile up against a finite sink (e.g. \code{1 / natural1()} approaching \code{0}), the
sink is the relevant bound even though it is not itself a discrete value.

\code{all()} and \code{any()} are not defined for numeric series, and \code{sum()} and
\code{prod()} throw an error when the series has infinitely many discrete values.
}
\examples{
range(natural1())          # 1 Inf
min(integers(5, 10))       # 5
max(1 / natural1())        # 1
range(1 / natural1())      # 0 1  (sink at 0 bounds the series below)
sum(integers(1, 100))      # 5050
}
