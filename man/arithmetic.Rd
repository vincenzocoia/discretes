% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arithmetic.R
\name{arithmetic}
\alias{arithmetic}
\title{Arithmetic Series}
\usage{
arithmetic(representative, spacing, ..., n_left = Inf, n_right = Inf)
}
\arguments{
\item{representative}{Numeric scalar giving a known term in the progression.}

\item{spacing}{Non-negative numeric scalar describing the distance between
adjacent terms.}

\item{...}{Reserved for future extensions; must be empty.}

\item{n_left, n_right}{Non-negative counts (possibly \code{Inf}, the default)
describing how many steps exist to the left and right of \code{representative}.}
}
\value{
A numeric series (class \code{dsct_arithmetic}, inheriting from
\code{discretes}).
}
\description{
Construct an arithmetic progression, with possibly infinite values.
The progression is anchored at \code{representative} and extends \code{n_left} steps
to the left (decreasing values) and \code{n_right} steps to the right
(increasing values) with constant \code{spacing} between consecutive terms.
}
\note{
While \code{spacing} can be zero, this results in a numeric series
containing only the \code{representative} value as its single discrete value.

The series can only contain \code{-0} if the \code{representative} is set as such.
}
\examples{
arithmetic(representative = -0.6, spacing = 0.7)
arithmetic(representative = 0.6, spacing = 0.7, n_right = 0)
arithmetic(representative = 0, spacing = 2, n_left = 2, n_right = 2)

# Negative zero, resulting in `-Inf` upon inversion:
has_negative_zero(arithmetic(-0, 1))
}
\seealso{
\code{\link[=integers]{integers()}}
}
