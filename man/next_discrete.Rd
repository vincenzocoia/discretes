% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/next_discrete.R, R/prev_discrete.R
\name{next_discrete}
\alias{next_discrete}
\alias{prev_discrete}
\title{Traversing a numeric series}
\usage{
next_discrete(
  x,
  from,
  ...,
  n = 1L,
  include_from = FALSE,
  tol = sqrt(.Machine$double.eps)
)

prev_discrete(
  x,
  from,
  ...,
  n = 1L,
  include_from = FALSE,
  tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{x}{Numeric series
(\code{numeric} vector or object of class \code{"discretes"}).}

\item{from}{Reference value to start searching from; single numeric.}

\item{...}{Reserved for future extensions; must be empty.}

\item{n}{Number of discrete values to find; single positive integer.}

\item{include_from}{Should the \code{from} value be included
in the query? Single logical; defaults to \code{TRUE}.}

\item{tol}{Numerical tolerance when checking if a value is in the series.
Single non-negative numeric. See \code{next_discrete()} for details.}
}
\value{
A vector of sequential points starting from \code{from}, which is
included in the vector if it is a discrete value in the numeric series and
\code{include_from = TRUE}. The length of the vector is at most \code{n}, and
will return an empty numeric vector if there is no such discrete
value. \code{next_discrete()} is increasing, while \code{prev_discrete()} is
decreasing, so that earlier values are encountered sooner.
}
\description{
\code{next_discrete()} and \code{prev_discrete()} find the \code{n} discrete values
in a numeric series next to a reference point. \code{num_discretes()} finds
the number of discrete values within a range.
}
\details{
For a transformed or combined series (e.g. \code{1 / integers()}), traversal is
delegated to the base series, and \code{tol} is passed through to those calls.
This means that \code{tol} is only realized on the underlying series:
\itemize{
\item \strong{Numeric vector} (or \code{as_discretes(...)}): a value is a member if it is
within \code{tol} of a stored value.
\item \strong{Arithmetic series}: the implied step index (distance from the
representative in steps) is treated as an integer if it is within \code{tol}
of an integer.
See the \href{https://discretes.netlify.app/articles/tolerance}{Tolerance} vignette for examples.
}
}
\examples{
x <- integers(from = 2)
next_discrete(x, from = 1.3)
prev_discrete(x, from = 4, n = 10)
next_discrete(x - 0.7, from = 1.3, n = 4)
}
